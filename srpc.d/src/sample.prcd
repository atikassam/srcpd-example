version 0.1
package com.linowis.srpcd.example

data FileMetadata {
    name: string
    directory: boolean
}

class File {
    data FileMetadata
    rename(name: string): boolean
    remove(): boolean
    move(source: string, dest: string): boolean
    files(): File[]
}

service class DriveService {
    rootFolder(): File
}